<Window x:Class="Philta.Views.PhiltaView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Philta.Views"
        mc:Ignorable="d"
        Title="Philta" Height="450" Width="800" WindowStartupLocation="CenterScreen">
    <StackPanel>
        <Label Content="Original file path" />
        <TextBox Text="{Binding FilePathTextBox}"/>
        <Label Content="Destination directory" />
        <TextBox Text="{Binding DestinationDirectoryTextBox}"/>
        <Button Command="{Binding CopyButtonCommand}">Copy</Button>
        <Button Command="{Binding ClearButtonCommand}">Clear</Button>
        <Label Content="{Binding StatusLabel}" />
    </StackPanel>
</Window>
